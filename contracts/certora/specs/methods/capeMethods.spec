methods {
    function _.depositErc20(CAPE.RecordOpening,address) external => DISPATCHER(true);
    function _.submitCapeBlock(CAPE.CapeBlock) external => DISPATCHER(true);
    function _.getRootValue() external => DISPATCHER(true);
}