methods {
    function _.addRoot(uint256) external envfree => DISPATCHER(true);
    function _.containsRoot(uint256) external envfree => DISPATCHER(true);
    function _.checkContainsRoot(uint256) external envfree => DISPATCHER(true);
    function _.getRoot(uint256) external envfree => DISPATCHER(true);
    function _.getRootsMap(uint256) external envfree => DISPATCHER(true);
    function _.getWriteHead() external envfree  => DISPATCHER(true);
}